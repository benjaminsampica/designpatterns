namespace StrategyPattern
{
    public enum FlyBehavior
    {
        FlyWithWings,
        NoFly
    }

    public enum QuackBehavior
    {
        MuteQuack,
        NormalQuack,
        Squeak
    }
}
